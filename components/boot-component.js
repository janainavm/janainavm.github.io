/**
 * application: janavm
 * 
 */

define(['h-vue'], ({ H }) => {

  const render = H(function(h) {
    
    return h('div',
      h('h1', 'JANAINA VIANA')
    );

  })


return { version, data, render }

})